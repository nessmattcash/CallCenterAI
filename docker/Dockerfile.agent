FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt langdetect
COPY src/agent_api.py .
CMD ["uvicorn", "agent_api:app", "--host", "0.0.0.0", "--port", "8000"]